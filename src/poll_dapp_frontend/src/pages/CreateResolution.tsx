import React from 'react';
import ResolutionForm from '../components/ResolutionForm';

const CreateResolution: React.FC = () => {
    return (
        <div>
            <h1>Create a New Resolution</h1>
            <ResolutionForm />
        </div>
    );
};

export default CreateResolution;